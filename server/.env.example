PORT=5000
NODE_ENV=development

# Database Configuration
# Option 1: Use DATABASE_URL (for production/Render.com)
# DATABASE_URL=postgresql://username:password@host:port/database

# Option 2: Use individual config (for local development)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=kudimall_dev
DB_PASSWORD=your_database_password
DB_PORT=5432

# JWT Secret Key (change this in production)
JWT_SECRET=kudimall-secret-key-change-in-production

# Email Configuration for Seller Verification
# For csetechsolution.com domain emails, use SMTP settings from your hosting provider
EMAIL_USER=kudimall@csetechsolution.com
EMAIL_PASSWORD=your-email-password
EMAIL_HOST=mail.csetechsolution.com
EMAIL_PORT=587
EMAIL_SECURE=false

# Skip email verification (set to true for development/testing, false for production)
SKIP_EMAIL_VERIFICATION=false

# Frontend URL (update for production)
FRONTEND_URL=http://localhost:3000
